from settings import *

class AllSprites(pygame.sprite.Group):
    def __init__(self):
        super().__init__()                                                                                             # call the __init__() method of the parent or superclass of a class  # isn't needed now for code to function properly, but will eventually in the future
